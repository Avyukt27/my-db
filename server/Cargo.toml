[package]
name = "server"
version = "0.1.0"
edition = "2024"

[dependencies]
db = { version = "0.1.0", path = "../db" }
rand = "0.10.2"
tokio = { version = "1.53.1", features = ["full"] }

[lib]
path = "src/lib.rs"
